import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Notification.Demo',
  appName: 'andrroid',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
